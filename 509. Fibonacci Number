// https://leetcode.com/problems/fibonacci-number/
class Solution {
public:
    int fib(int N) {
        int z;
        int F [31];
        F[0] = 0;
        F[1] = 1;
        for(int i = 2; i < N +1; i++){
            F[i] = F[i-1] + F[i -2];
        }
        z = F[N];
        return z;
    }
};
